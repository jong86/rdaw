// @flow
export type appState = {
  global: {
    DeviceBar: {
      height: number,
    },
    TitleBar: {
      height: number,
    },
  },

  project: {
    title: string,
    tempo: number,
    timeSignature: {
      numerator: number,
      denominator: number,
    },
    swing: number,
    view: {
      zoom: number,
      left: number,
    },
  },

  tracks: Array<Object>,
}

export type note = {
  duration: number,
  startsAt: number,
  midiNum: number,
}

export type rectDimensions = {
  width: number,
  height: number,
}

export type track = {
  id: number,
  type: string,
  instrument: string,
  isArmed: boolean,
}

